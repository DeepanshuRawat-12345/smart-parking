[phases.setup]
nixPkgs = ["python311"]
 
[phases.install]
cmds = [
  "cd backend && python3 -m venv venv && venv/bin/pip install -r requirements.txt"
]
 
[start]
cmd = "cd backend && venv/bin/uvicorn app:app --host 0.0.0.0 --port $PORT"
